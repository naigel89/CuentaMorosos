*.iml
.gradle/
.idea/
local.properties
/build
/captures
.externalNativeBuild
.cxx
.DS_Store
app/build/
shared/build/

# SDD artifacts (local, not shareable)
.atl/

# Keystore — committed for CI release signing
# *.jks removed: CuentaMorosos.jks is needed in CI for assembleRelease

# Firebase config files (sensitive — added via GitHub Secrets in CI)
app/google-services.json
iosApp/GoogleService-Info.plist
