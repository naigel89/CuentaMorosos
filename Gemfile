source "https://rubygems.org"

# Keep CocoaPods reproducible across developer Macs and CI. Generate and commit
# Gemfile.lock only from the approved macOS toolchain during Gate A.
gem "cocoapods", "1.16.2"
